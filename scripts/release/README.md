# Scripts de release

À créer seulement lorsque le workflow de release aura été spécifié.

Les futurs scripts devront dériver la version depuis `VERSION` et générer les
noms d'artefacts sans recopier manuellement le numéro ailleurs.
